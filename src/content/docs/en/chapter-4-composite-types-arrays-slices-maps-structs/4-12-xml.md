---
title: XML
sidebar:
  order: 12
  label: 4.12 XML
---

## Context

The `encoding/xml` package provides XML marshaling and unmarshaling similar to JSON. Struct tags specify element names and attributes.

## Example

Marshal a struct to XML.

## Code example

```go
package main

import (
    "encoding/xml"
    "fmt"
)

type Person struct {
    XMLName xml.Name `xml:"person"`
    Name    string   `xml:"name"`
    Age     int      `xml:"age"`
}

func main() {
    p := Person{Name: "Alice", Age: 30}
    data, _ := xml.MarshalIndent(p, "", "  ")
    fmt.Println(string(data))
}
```

## Output

```bash
<person>
  <name>Alice</name>
  <age>30</age>
</person>
```

## Useful links

- [xml package documentation](https://pkg.go.dev/encoding/xml)
- [Go by Example: XML](https://gobyexample.com/xml)
